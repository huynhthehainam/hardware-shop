version: "3.8"
services:

  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=Doyouloveme1029!
      - MSSQL_PID=Express
  migration:
    build:
      context: .
      dockerfile: HardwareShop.DatabaseMigration/Dockerfile.dev
    networks:
      - api-shop

  web-api:
    deploy:
      replicas: 0

