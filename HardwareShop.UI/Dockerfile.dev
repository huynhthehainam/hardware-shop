FROM node:22-bullseye

WORKDIR /app

# Copy entire repo so build works when context is repo root
COPY . .

WORKDIR /app/HardwareShop.UI
RUN corepack enable \
  && corepack prepare yarn@1.22.22 --activate \
  && yarn install

EXPOSE 4200
